[sandbox]  # Docker-based sandbox host (not your local machine)
sandbox_webserver ansible_connection=docker ansible_python_interpreter=/usr/bin/python3 # Run tasks inside this container via `docker exec`
